# 🚀 دليل تشغيل نظام AI Cyber Defender (للعرض على اللجنة)

هذا الدليل يوضح الأوامر باختصار شديد لتشغيل النظام بالكامل وعرض المحاكي في الوقت الحقيقي (Real-Time).

---

## 1️⃣ تشغيل بيئة النظام الأساسية (السيرفرات)
في نافذة تيرمينال **(النافذة الأولى)**، قم بتشغيل سكربت التشغيل الموحد لجميع الخدمات (Backend + AI + Frontend):
```bash
cd ~/Documents/my_prjects/my_prjects/AI-Cyber-Defender
./start_all.sh
```
> *(انتظر حتى تعمل جميع الخدمات وتظهر لك رسائل النجاح الخضراء)*

---

## 2️⃣ فتح لوحة التحكم (الداشبورد)
افتح المتصفح الخاص بك (Google Chrome مثلاً) وادخل على الرابط التالي:
👉 **http://localhost:4200**

*(قم بتسجيل الدخول، وتوجه إلى صفحة `Dashboard` أو `Live Alerts`)*

---

## 3️⃣ تشغيل مراقب السجلات (Log Agent)
هذا هو النظام الذي يراقب السجلات بشكل حي ويبعثها للذكاء الاصطناعي.
افتح نافذة تيرمينال جديدة **(النافذة الثانية)**، واكتب:
```bash
cd ~/Documents/my_prjects/my_prjects/AI-Cyber-Defender
python3 log_agent.py
```
> *(اترك هذه النافذة مفتوحة لترى كيف يقوم باصطياد الهجمات الحية)*

---

## 4️⃣ تشغيل مُحاكي الهجمات (لإبهار اللجنة!) 🎯
الآن، لتبدأ العرض الفعلي أمام اللجنة، افتح نافذة تيرمينال جديدة **(النافذة الثالثة)**، واكتب:
```bash
cd ~/Documents/my_prjects/my_prjects/AI-Cyber-Defender
./test_logs.sh
```

**ماذا سيحدث الآن؟**
- سيبدأ المحاكي بضخ عشرات الهجمات بسرعة كبيرة وبشكل مستمر (مثل DDoS و SQLi و Brute Force).
- ستتغير الأرقام في المتصفح **أوتوماتيكياً (Real-Time)** وتزيد الهجمات أمام عين اللجنة.
- سيبدأ شريط الـ Agent بالوميض باللون الأحمر لكل هجوم خطير.

🛑 **لإيقاف المحاكي بعد انتهاء العرض:**
فقط اضغط على **`Ctrl + C`** في نافذة المحاكي.
